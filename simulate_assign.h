namespace SimulateAssign
{	
	
	vector<double> analyse(Profile p, vector<int> v, char rule, string output);
	RankMatrix analyse_rm(Profile p, vector<int> v, char rule);
	void simulate(int N, int n, double phi);
	void simulate_rm(int N, int n, double phi);		
}
