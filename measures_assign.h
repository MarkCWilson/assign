#include "rankmatrix.h"
namespace MeasuresAssign
{
	RankMatrix rankdist(Profile p, Assign q);	
	double utilwelf (vector<double> u);
	double egalwelf (vector<double> u);
	double maxwelf (vector<double> u);
	double nashwelf (vector<double> u);
	double orderbias (vector<double> u);
	double obmatnorm (RankMatrix M);
};